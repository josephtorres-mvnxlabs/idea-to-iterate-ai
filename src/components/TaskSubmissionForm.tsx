
// Re-export the TaskSubmissionForm from the new path for backwards compatibility
// This prevents breaking existing imports

import { TaskSubmissionForm } from "./task-form/TaskSubmissionForm";

export { TaskSubmissionForm };
